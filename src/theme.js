import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#99e2bb",
    },
    secondary: {
      main: "#1aaf6c",
    },
  },
});

export default theme;
